---
import { ClientRouter } from 'astro:transitions';
import '../styles/global.css';
import { SITE_INFO } from '@consts/site-info';
import Navbar from '@components/shared/navbar.astro';


interface Props {
	title?: string;
	description?: string;
	image?: string;
	favicon?: string;
}
const { title = SITE_INFO.title, description = SITE_INFO.description, image, favicon } = Astro.props;
---

<html lang="es" data-theme="dark">
	<head>
		<meta charset="utf-8" />
		
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<ClientRouter />
		<!-- Zero-Javascript View-Transitions -->
		<!-- <style>@view-transition { navigation: auto; }</style> -->
		<title>{title}</title>

        {favicon ? <link rel="icon" type="image/png" href={favicon} /> : <link rel="icon" type="image/svg+xml" href="/favicon.svg" />}

		<!-- SEO -->
		<meta name="description" content={description} />
		<meta name="author" content="Mauro Daniel Viveros" />

		<!-- OG -->
		<meta property="og:title" content={title} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={image} />

	</head>
	<body class="bg-slate-500 text-slate-100 scheme-dark">
		<header>
			<Navbar />
		</header>
        <main class="mt-10 m-auto max-w-3xl">
            <slot />
        </main>
	</body>
</html>
